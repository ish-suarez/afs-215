const expect = require('chai').assert
const fizzBuzz = require('../fizzBuzzKata')

it('fizzbuzz returns 1', () => {
    expect.equal(fizzBuzz(1), 1)
})
it('fizzbuzz returns 2', () => {
    expect.equal(fizzBuzz(2), 2)
}) 
